export default function AiBrief({ brief, score }) {
  if (!brief) return null;

  return (
    <div style={S.card}>
      <div style={S.header}>
        <div style={S.titleRow}>
          <div style={S.title}>AI Retention Strategy Brief</div>
          {score !== null && (
            <div style={S.score}>
              Quality Score: <span style={S.scoreVal}>{(score * 100).toFixed(0)}%</span>
            </div>
          )}
        </div>
        <div style={S.sub}>Generated by LangGraph · LlamaIndex RAG · Groq + Llama</div>
      </div>

      <div style={S.assessment}>{brief.risk_assessment}</div>

      <div style={S.grid}>
        <div style={S.section}>
          <div style={S.sectionLabel}>Top 3 Retention Actions</div>
          {(brief.top_actions || []).map((action, i) => (
            <div key={i} style={S.action}>
              <span style={S.actionNum}>{i + 1}</span>
              <span style={S.actionText}>{action}</span>
            </div>
          ))}
        </div>

        <div style={S.section}>
          <div style={S.sectionLabel}>Recommended Offer</div>
          <div style={S.offer}>{brief.recommended_offer}</div>

          <div style={S.sectionLabel}>Estimated Saved Revenue</div>
          <div style={{ ...S.offer, color: "#00FF8C" }}>{brief.estimated_saved_revenue}</div>
        </div>
      </div>

      <div style={S.bottomLine}>
        <span style={S.bottomLineLabel}>Bottom Line</span>
        <span style={S.bottomLineText}>{brief.bottom_line}</span>
      </div>
    </div>
  );
}

const S = {
  card: { background: "#111", border: "1px solid #1A1A1A", borderRadius: 14, padding: "28px" },
  header: { marginBottom: 20 },
  titleRow: { display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 6 },
  title: { fontSize: 14, fontWeight: 600, letterSpacing: "-0.01em" },
  score: { fontSize: 12, color: "#555" },
  scoreVal: { color: "#00FF8C", fontWeight: 700 },
  sub: { fontSize: 12, color: "#333" },
  assessment: {
    background: "#0D0D0D", border: "1px solid #1E1E1E", borderRadius: 8,
    padding: "14px 16px", fontSize: 14, color: "#888", lineHeight: 1.6, marginBottom: 24,
  },
  grid: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 24, marginBottom: 20 },
  section: { display: "flex", flexDirection: "column", gap: 10 },
  sectionLabel: { fontSize: 10, letterSpacing: "0.12em", color: "#333", textTransform: "uppercase", marginTop: 8 },
  action: { display: "flex", gap: 12, alignItems: "flex-start" },
  actionNum: {
    width: 22, height: 22, borderRadius: "50%", background: "#1A1A1A",
    border: "1px solid #333", display: "flex", alignItems: "center",
    justifyContent: "center", fontSize: 11, color: "#555", flexShrink: 0,
  },
  actionText: { fontSize: 13, color: "#888", lineHeight: 1.6 },
  offer: { fontSize: 18, fontWeight: 600, color: "#FFD166", letterSpacing: "-0.01em" },
  bottomLine: {
    background: "#0A150E", border: "1px solid #0F2E18", borderRadius: 8,
    padding: "14px 16px", display: "flex", gap: 12, alignItems: "flex-start",
  },
  bottomLineLabel: {
    fontSize: 10, letterSpacing: "0.1em", textTransform: "uppercase",
    color: "#00FF8C", flexShrink: 0, paddingTop: 2,
  },
  bottomLineText: { fontSize: 14, color: "#888", lineHeight: 1.6 },
};
